#pragma once

#include <QIcon>
#include <QString>

namespace flatlas::ui {

QIcon toolIcon(const QString &key);
QIcon fileIcon();
QIcon saveIcon();
QIcon settingsIcon();
QIcon helpIcon();
QIcon launchIcon();

} // namespace flatlas::ui
